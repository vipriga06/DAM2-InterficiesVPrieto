import 'package:flutter/material.dart';

class EncryptView extends StatelessWidget {
  const EncryptView({super.key});

    @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('View encrypt'),
    );
  }
}
