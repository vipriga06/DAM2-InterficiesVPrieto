import 'package:flutter/material.dart';

class DecryptView extends StatelessWidget {
  const DecryptView({super.key});

    @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('view decrypt'),
    );
  }
}